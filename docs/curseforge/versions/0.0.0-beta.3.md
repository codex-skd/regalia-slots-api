<h2>v0.0.0-beta.3 - License file cleanup</h2>

<h3>Changed</h3>
<ul>
<li>Removed <code>COPYING</code> (GPL text) and <code>LICENSE</code> (short summary) from the repository and the built JAR. Only <code>COPYING.LESSER</code> (the LGPL text itself) is kept, since that's the minimum required by the license.</li>
</ul>

<blockquote>No API or gameplay behavior changes in this release — packaging/licensing cleanup only.</blockquote>
